{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "{{ author }}",
  "license": "ISC",
  "bin": {
    "mycli": "index.js"
  },
  "dependencies": {
    "commander": "^7.2.0",
    "download-git-repo": "^3.0.2",
    "inquirer": "^8.1.0"
  }
}
